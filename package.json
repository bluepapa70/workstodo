{
  "name": "workstodo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute workstodo-db --file=schema.sql",
    "db:init:remote": "wrangler d1 execute workstodo-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "wrangler": "^3.0.0"
  }
}
